module.exports =

  # Define what processes in the Protocol we are extending.
  extends:
    run: ['server.test/run', 'server.run/run']

  # These services are responsible for initializing our Extension,
  # loading any necessary code from the user's domain, and defining
  # the runtime behavior of the Extension.
  services:
    run: (args, done) ->
      done()
